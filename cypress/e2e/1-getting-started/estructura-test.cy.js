
beforeEach(() => {
    cy.visit('www.algo.com');
});



describe('Login', () => {
    context('cuando el usuario ingresa con credenciales validas', () => {
    it('deberia de hacer login exitoso', () => {
        
        });
    it('deberia de mostrar el panel del inicio del usuario', () => {
        
        });
    it('login fallido', () => {
        
        });
    });

});

describe('Login cuando el usuario ingresa con credenciales validas', () => {
    it('deberia de hacer login exitoso', () => {
        
        });
    it('deberia de mostrar el panel del inicio del usuario', () => {
        
        });
});




